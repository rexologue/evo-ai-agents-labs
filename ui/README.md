# UI

Веб-интерфейс для диалога с агентом CompanyProfiler через протокол A2A. Клиент позволяет общаться с агентом, просматривать сохранённые профили и запускать приложение в контейнере так же, как другие подпроекты.

## 🔧 Переменные окружения
- `A2A_BASE_URL` — адрес A2A-агента CompanyProfiler (по умолчанию `http://localhost:28003`).

## 🚀 Запуск в контейнере
```bash
docker buildx build --platform linux/amd64 -t ui -f Dockerfile ..
docker run --rm --network host --env-file .env ui
```

Приложение будет доступно по адресу `http://localhost:8000`. Убедитесь, что A2A-агент запущен и доступен по `A2A_BASE_URL`.
