Вот готовый **README.md** (можно копировать целиком):

````markdown
# 🧠 AI Agents on GosPlan API (A2A + MCP)

> От «мы хотим такие-то закупки» → к сохранённому профилю компании → к короткому ранжированному шорт-листу закупок.

Этот репозиторий — **набор микросервисов** для платформы AI Agents, где:
- **LLM-агенты** ведут диалог и принимают решения *как навигатор*,
- **MCP-сервисы** дают факты/данные и делают действие *как двигатель* (справочники, БД, API закупок).

Главная идея: **не заставлять модель “помнить и выдумывать”**, а вынести источники истины в инструменты, чтобы ответы были:
- проверяемыми,
- воспроизводимыми,
- машинно-читаемыми,
- и удобно расширяемыми.

---

## Зачем это устроено именно так

### 1) Чтобы “зачем” было сильнее “как”
В закупках ключевое — не красивый текст, а **правильная постановка фильтров** и **контекст компании**:
- что компания реально делает (ОКПД2),
- где работает (коды регионов),
- какие сроки критичны (окончание подачи заявок),
- какие ограничения/предпочтения есть.

LLM отлично понимает намерение, но **плохо справляется с ролью базы знаний и реестра**. Поэтому знание и действия вынесены в MCP.

### 2) Чтобы уменьшить галлюцинации и “фейковые ссылки”
Ссылки, идентификаторы, карточки закупок, документы — всё это должно приходить **из источника**, а не из фантазии модели.
Поэтому:
- MCP возвращает **structured_content** (для программной обработки),
- и **человеческий текст** (для удобного чтения),
- а агенты лишь собирают параметры и вызывают инструменты.

### 3) Чтобы любой компонент можно было заменить без боли
- другой LLM / другой провайдер,
- другой источник закупок,
- другой формат хранения профилей,
- новые инструменты,
- новые агенты под новые сценарии.

---

## Концептуальная схема сервиса

```mermaid
flowchart LR
  U[Пользователь] -->|диалог| AP[Agent: CompanyProfiler]
  U -->|диалог| AM[Agent: PurchaseMatcher]

  AP -->|get_okpd2_codes / get_regions_codes| CM[codes-mcp]
  AP -->|create_company_profile / get_company_profile| DBM[db-mcp]
  DBM --> PG[(PostgreSQL)]

  AM -->|get_company_profile / list_company_profiles| DBM
  AM -->|search_purchases / get_purchase_details| GM[gosplan-mcp]
  GM --> GP[ГосПлан API]
````

### “Золотой путь” (как это ощущается пользователю)

1. **CompanyProfiler** превращает “разрозненные слова” пользователя в **аккуратный профиль компании** и сохраняет его.
2. **PurchaseMatcher** берёт профиль + запрос пользователя и выдаёт **ранжированный список закупок**, который можно дальше:

   * сохранять,
   * фильтровать,
   * отправлять в UI,
---

## Компоненты репозитория

### 🤖 Агенты (A2A)

* **agent-profiler** — *CompanyProfiler*: собирает профиль компании (RU-диалог), подбирает коды ОКПД2 и регионов через `codes-mcp`, сохраняет профиль в `db-mcp`.
* **agent-purchaser** — *PurchaseMatcher*: собирает запрос на закупки, подтягивает профиль из `db-mcp`, ищет закупки через `gosplan-mcp`, ранжирует и отдаёт результат.

### 🧰 MCP-сервисы (FastMCP, streamable-http)

* **codes-mcp** — справочники:

  * ОКПД2 (CSV → память),
  * регионы (CSV → память).
* **db-mcp** — хранение профилей компаний в PostgreSQL:

  * создание профиля,
  * получение по UUID,
  * список с поиском и пагинацией.
* **gosplan-mcp** — прокси к API ГосПлан:

  * поиск закупок по классификаторам/регионам/датам,
  * детальная карточка закупки с форматированием.

### 🗄️ База данных

* **PostgreSQL** (docker-compose лежит в `db/docker-compose.yaml`).

---

## Форматы взаимодействия (важно для “зачем”)

### CompanyProfiler: “профиль как контракт”

Профиль — это не текст “для красоты”, а **контракт**, который:

* переиспользуется в следующих диалогах,
* обеспечивает стабильную фильтрацию закупок,
* и позволяет строить дальнейшую автоматизацию.

Финальный ответ CompanyProfiler — **строгий JSON** (без Markdown и лишнего текста):

```json
{
  "company_name": "…",
  "company_id": "…"
}
```

Подтверждение пользователем поддерживает короткие ответы (включая “газ” как сильное “да”).

### PurchaseMatcher: “результат как поток”

PurchaseMatcher — удобно:

* писать в файл,
* стримить в UI,
* индексировать,
* пост-обрабатывать скоринг.

---

## Быстрый старт (локально, Docker)

### 0) Требования

* Docker + buildx
* (Linux/WSL2) желательно, т.к. ниже используется `--network host`

### 1) Поднять PostgreSQL

```bash
docker compose -f db/docker-compose.yaml up -d
```

### 2) Подготовить env-файлы

Порт и host можно менять — ниже типовая раскладка “всё на localhost”.

**env/db.env**

```env
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=postgres
```

**env/db-mcp.env**

```env
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=postgres
DB_MCP_HOST=0.0.0.0
DB_MCP_PORT=28001
LOG_LEVEL=INFO
```

**env/codes-mcp.env**

```env
MCP_HOST=0.0.0.0
MCP_PORT=28002
LOG_LEVEL=INFO
```

**env/gosplan-mcp.env**

```env
MCP_HOST=0.0.0.0
MCP_PORT=28003
LOG_LEVEL=INFO
GOSPLAN_BASE_URL=https://v2test.gosplan.info
GOSPLAN_TIMEOUT=20
GOSPLAN_PURCHASES_LIMIT=9
```

**env/agent-profiler.env**

```env
LLM_MODEL=hosted_vllm/Qwen/Qwen3-14B-AWQ
LLM_API_BASE=http://127.0.0.1:28000/v1
LLM_API_KEY=CHANGE_ME

DB_MCP_URL=http://127.0.0.1:28001
CODES_MCP_URL=http://127.0.0.1:28002

AGENT_HOST=0.0.0.0
AGENT_PORT=8001
LOG_LEVEL=INFO
```

**env/agent-purchaser.env**

```env
LLM_MODEL=hosted_vllm/Qwen/Qwen3-14B-AWQ
LLM_API_BASE=http://127.0.0.1:28000/v1
LLM_API_KEY=CHANGE_ME

DB_MCP_URL=http://127.0.0.1:28001
GOSPLAN_MCP_URL=http://127.0.0.1:28003

AGENT_HOST=0.0.0.0
AGENT_PORT=8003
LOG_LEVEL=INFO
```

### 3) Собрать образы

```bash
cd db-mcp && docker buildx build --platform linux/amd64 -t db-mcp -f Dockerfile ..
```

```bash
cd codes-mcp && docker buildx build --platform linux/amd64 -t codes-mcp -f Dockerfile ..
```

```bash
cd gosplan-mcp && docker buildx build --platform linux/amd64 -t gosplan-mcp -f Dockerfile ..
```

```bash
cd agent-profiler && docker buildx build --platform linux/amd64 -t agent-profiler -f Dockerfile ..
```

```bash
cd agent-purchaser && docker buildx build --platform linux/amd64 -t agent-purchaser -f Dockerfile ..
```

### 4) Запустить сервисы

```bash
docker run --rm --network host --env-file env/db-mcp.env db-mcp
```

```bash
docker run --rm --network host --env-file env/codes-mcp.env codes-mcp
```

```bash
docker run --rm --network host --env-file env/gosplan-mcp.env gosplan-mcp
```

```bash
docker run --rm --network host --env-file env/agent-profiler.env agent-profiler
```

```bash
docker run --rm --network host --env-file env/agent-purchaser.env agent-purchaser
```

---

## Эндпоинты (что проверять первым)

### MCP-сервисы (инструменты)

* `http://127.0.0.1:28001/mcp` — db-mcp
* `http://127.0.0.1:28002/mcp` — codes-mcp
* `http://127.0.0.1:28003/mcp` — gosplan-mcp

У MCP сервисов также есть `/health` и `/metrics` (удобно для диагностики и мониторинга).

### A2A-агенты (диалог)

* `http://127.0.0.1:8001/.well-known/agent-card.json` — CompanyProfiler
* `http://127.0.0.1:8003/.well-known/agent-card.json` — PurchaseMatcher

---

## Как оно устроено внутри (коротко, но по делу)

### Общий принцип

1. **Settings** читаются из env (Pydantic Settings).
2. **MCP** сервисы поднимаются на FastMCP и отдают `ToolResult`:

   * `content` — человекочитаемо,
   * `structured_content` — для агентов/клиентов.
3. **Агенты**:

   * подключают MCP через MultiServer MCP client,
   * создают tool-calling агента (LangChain classic),
   * оборачивают всё в A2A (Starlette application),
   * стримят ответы и чистят служебные блоки.

### Почему в репо два агента, а не один “суперагент”

Это про **качественную декомпозицию смысла**:

* CompanyProfiler — “кристаллизация контекста” (профиль как актив),
* PurchaseMatcher — “поиск + интерпретация” (результат как поток).

Так проще развивать и тестировать, а пользователю понятнее, что происходит.

---

## Отладка и диагностика

### MCP Inspector

MCP сервисы включают CORS-настройки под инспектор (удобно для ручной проверки инструментов).

### Типовые проблемы

* **Агент не стартует и ругается на missing tools**: почти всегда это неверные `*_MCP_URL` или сеть/DNS, потому что агент принципиально не запускается без обязательных инструментов.
* **Лимиты ГосПлан**: регулируются `GOSPLAN_PURCHASES_LIMIT` (по умолчанию 4+4).

---

## Куда расширять дальше (естественная эволюция)

* Добавить MCP-инструмент “сохранить результаты матчинг-сессии” в `db-mcp`.
* Добавить агент “TenderBrief” (сжатое резюме закупки + чеклист участия).
* Вынести скоринг в отдельный сервис/модуль и сделать A/B сравнение формул.
* Подключить наблюдаемость (OpenInference / Phoenix) как стандартный трейсинг диалогов и tool-calls.

---
