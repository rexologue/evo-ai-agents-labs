# evo-ai-agents-labs

Набор сервисов и A2A-агентов вокруг MCP: справочники кодов, поиск закупок и работа с профилями компаний.
Документы выровнены между собой по структуре `db-mcp` и дополняют её краткими заметками об имплементации.

## 📦 Компоненты
- **codes-mcp** — выдаёт справочники ОКПД2 и регионов из локальных CSV.
- **gosplan-mcp** — ищет и возвращает закупки через API ГосПлан.
- **db-mcp** — сохраняет и отдаёт профили компаний в PostgreSQL.
- **agent-profiler** — A2A-агент для диалогового сбора профиля компании.
- **agent-purchaser** — A2A-агент, который подбирает закупки по готовому профилю.

## 🔧 Переменные окружения
Каждый сервис содержит `env_options.json` и пример `.env.example` с обязательными и опциональными
переменными. Минимальный набор:
- MCP-сервера (`codes-mcp`, `gosplan-mcp`, `db-mcp`): `MCP_HOST`, `MCP_PORT`, подключение к БД или внешним API.
- Агенты (`agent-profiler`, `agent-purchaser`): `LLM_MODEL`, `LLM_API_KEY`, `LLM_API_BASE`, адреса MCP-сервисов
  и параметры собственного A2A HTTP-сервера (`AGENT_HOST`/`AGENT_PORT`/`AGENT_URL`).

## 🚀 Запуск
Каждый модуль собирается и запускается одинаково:
```bash
docker buildx build --platform linux/amd64 -t <имя-сервиса> -f Dockerfile ..
docker run --rm --network host --env-file .env <имя-сервиса>
```
Адреса MCP-транспорта или A2A-серверов берутся из соответствующих переменных окружения.

## 🛠️ Имплементационные заметки
- Все MCP-сервисы работают через transport `streamable-http` и описывают инструменты в `mcp_tools.json`.
- Данные кодов (codes-mcp) подгружаются из локальных CSV и кэшируются в памяти для быстрых ответов.
- `db-mcp` использует PostgreSQL с пэйджингом и поиском по профилям; UUID компании передаются как строки.
- Агенты используют LangChain стеки и вызывают MCP-инструменты как внешние сервисы, не дублируя бизнес-логику.
