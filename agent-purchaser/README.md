# agent-profiler

LangChain агент «CompanyProfiler» для формирования профилей компаний. Агент общается через A2A, использует MCP-сервера `db-mcp`
и `codes-mcp` и готов для развёртывания в Docker.

## 🚀 Возможности
- Сбор профиля компании через диалог на русском языке.
- Подбор кодов регионов и ОКПД2 через MCP-инструменты `codes-mcp`.
- Сохранение подтверждённого профиля компании через `db-mcp`.

## 🔧 Переменные окружения
Основные параметры описаны в `env_options.json`. Ключевые переменные:
- `LLM_MODEL`, `LLM_API_KEY`, `LLM_API_BASE` — настройки LLM.
- `DB_MCP_URL`, `CODES_MCP_URL` — адреса MCP-серверов с базой профилей и справочниками.
- `AGENT_HOST`, `AGENT_PORT`, `AGENT_URL` — адресация A2A-сервера агента.

## 🚀 Запуск
```bash
docker buildx build --platform linux/amd64 -t agent-purchaser -f Dockerfile ..
docker run --rm --network host --env-file .env agent-purchaser
```

Сервер будет доступен по `AGENT_URL` (по умолчанию `http://0.0.0.0:8001`).

## 📖 MCP / Agent инструменты
Агент использует инструменты MCP-сервисов `db-mcp` и `codes-mcp` (описаны в их `mcp_tools.json`).
