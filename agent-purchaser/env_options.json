{
  "environment_variables": [
    {
      "name": "LOG_LEVEL",
      "type": "string",
      "default": "INFO",
      "description": "Уровень логирования (DEBUG, INFO, WARNING, ERROR, CRITICAL)",
      "required": false,
      "validation": {
        "enum": ["DEBUG", "INFO", "WARNING", "ERROR", "CRITICAL"]
      },
      "example": "INFO"
    },
    {
      "name": "LLM_MODEL",
      "type": "string",
      "default": "",
      "description": "Название модели LLM для обработки диалогов",
      "required": true,
      "example": "gpt-4o-mini"
    },
    {
      "name": "LLM_API_KEY",
      "type": "string",
      "default": "",
      "description": "API-ключ для доступа к выбранной LLM",
      "required": true,
      "validation": {
        "minLength": 1
      },
      "example": "sk-***"
    },
    {
      "name": "LLM_API_BASE",
      "type": "string",
      "default": "",
      "description": "Базовый URL провайдера LLM",
      "required": true,
      "validation": {
        "format": "uri"
      },
      "example": "https://api.openai.com/v1"
    },
    {
      "name": "DB_MCP_URL",
      "type": "string",
      "default": "",
      "description": "HTTP URL MCP-сервера для работы с профилями компаний (db-mcp)",
      "required": true,
      "validation": {
        "format": "uri"
      },
      "example": "http://db-mcp:8000"
    },
    {
      "name": "GOSPLAN_MCP_URL",
      "type": "string",
      "default": "",
      "description": "HTTP URL MCP-сервера для работы с закупками (gosplan-mcp)",
      "required": true,
      "validation": {
        "format": "uri"
      },
      "example": "http://gosplan-mcp:8000"
    },
    {
      "name": "AGENT_NAME",
      "type": "string",
      "default": "PurchaseMatcher",
      "description": "Отображаемое имя A2A-агента",
      "required": false,
      "example": "PurchaseMatcher"
    },
    {
      "name": "AGENT_DESCRIPTION",
      "type": "string",
      "default": "Агент подбора закупок по профилю компании",
      "description": "Описание агента в A2A-карточке",
      "required": false,
      "example": "Агент подбирает релевантные закупки по профилю компании через db-mcp и gosplan-mcp"
    },
    {
      "name": "AGENT_HOST",
      "type": "string",
      "default": "0.0.0.0",
      "description": "Хост для запуска A2A HTTP-сервера",
      "required": false,
      "validation": {
        "format": "hostname"
      },
      "example": "0.0.0.0"
    },
    {
      "name": "AGENT_PORT",
      "type": "integer",
      "default": 8003,
      "description": "Порт A2A HTTP-сервера",
      "required": false,
      "validation": {
        "min": 1,
        "max": 65535
      },
      "example": 8003
    },
    {
      "name": "AGENT_URL",
      "type": "string",
      "default": "",
      "description": "Полный URL агента (если не указан, собирается из AGENT_HOST и AGENT_PORT)",
      "required": false,
      "validation": {
        "format": "uri"
      },
      "example": "http://0.0.0.0:8003"
    }
  ],
  "metadata": {
    "version": "1.0.0",
    "description": "Переменные окружения для запуска агента PurchaseMatcher через A2A",
    "usage": {
      "docker": "Передавайте переменные через --env-file или -e",
      "local": "Создайте .env в корне agent-purchaser или экспортируйте переменные в окружение"
    },
    "notes": [
      "LLM_* обязательны для работы агента через LangChain",
      "DB_MCP_URL должен указывать на db-mcp",
      "GOSPLAN_MCP_URL должен указывать на gosplan-mcp",
      "AGENT_URL вычисляется автоматически, если не задан"
    ]
  }
}
