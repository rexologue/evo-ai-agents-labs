# gosplan-mcp

MCP сервер для поиска и получения деталей государственных закупок из ГосПлан.

## 🚀 Возможности
- `search_purchases` — поиск закупок по ОКПД2, регионам и датам окончания подачи заявок. Работает по 44-ФЗ и 223-ФЗ, для каждого
  закона запрашивает объем из `GOSPLAN_PURCHASES_LIMIT`. Всегда ищет рублевые закупки на этапе подачи заявок, сортирует по `updated_at_desc`.
- `get_purchase_details` — получение полной информации о закупке по номеру с автоматическим определением закона (или явным указанием 44-ФЗ/223-ФЗ).

## 🔧 Переменные окружения

Все переменные задаются через `.env` (см. `.env.example`) или флаги контейнера.

- `MCP_HOST` — хост HTTP транспорта MCP (обязательно)
- `MCP_PORT` — порт HTTP транспорта MCP (обязательно)
- `LOG_LEVEL` — уровень логирования (`DEBUG`/`INFO`/`WARNING`/`ERROR`/`CRITICAL`)
- `GOSPLAN_BASE_URL` — базовый URL API ГосПлан (по умолчанию `https://v2test.gosplan.info`)
- `GOSPLAN_TIMEOUT` — таймаут HTTP-запросов к ГосПлан в секундах (по умолчанию `20`)
- `GOSPLAN_PURCHASES_LIMIT` — сколько закупок выдавать за один вызов инструмента (по умолчанию `9`)

## 🚀 Запуск
```bash
docker buildx build --platform linux/amd64 -t gosplan-mcp -f Dockerfile ..
docker run --rm --network host --env-file .env gosplan-mcp
```

После старта сервер доступен по адресу `http://<MCP_HOST>:<MCP_PORT>/mcp` с транспортом `streamable-http`.

## 🛠️ Как работает
- Клиентские запросы проксируются в публичный API ГосПлан с учётом таймаута и лимита ответов.
- Поиск всегда возвращает актуальные закупки в стадии подачи заявок, фильтрует по валюте `RUB` и сортирует по `updated_at_desc`.
- Детальная карточка дополнительно форматирует данные из `structured_content` для удобства MCP-клиентов.

## 📖 Использование инструментов
- `search_purchases(classifiers[], region_codes[], collecting_finished_after?, collecting_finished_before?)` — поиск списка закупок по 44-ФЗ/223-ФЗ с фиксированными параметрами: stage=1, currency=RUB, сортировка `updated_at_desc`, всегда по обоим законам.
- `get_purchase_details(purchase_number, law?)` — детальная карточка закупки с pretty-форматированием.

Полное описание аргументов доступно в `mcp_tools.json`.

## Выгрузка на Cloud.ru
```bash
docker tag gosplan-mcp:latest your-artifact-registry.cloud.ru/gosplan-mcp-repo:v1.0.0
docker push your-artifact-registry.cloud.ru/gosplan-mcp-repo:v1.0.0
```