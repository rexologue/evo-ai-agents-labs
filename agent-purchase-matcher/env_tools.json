{
  "environment_variables": [
    {
      "name": "LOG_LEVEL",
      "type": "string",
      "default": "INFO",
      "description": "Уровень логирования (DEBUG, INFO, WARNING, ERROR, CRITICAL)",
      "required": false,
      "validation": {
        "enum": ["DEBUG", "INFO", "WARNING", "ERROR", "CRITICAL"]
      },
      "example": "INFO"
    },
    {
      "name": "LLM_MODEL",
      "type": "string",
      "default": "",
      "description": "Название модели LLM для обработки диалогов",
      "required": true,
      "example": "gpt-4o-mini"
    },
    {
      "name": "LLM_API_KEY",
      "type": "string",
      "default": "",
      "description": "API-ключ для доступа к выбранной LLM",
      "required": true,
      "validation": {
        "minLength": 1
      },
      "example": "sk-***"
    },
    {
      "name": "LLM_API_BASE",
      "type": "string",
      "default": "",
      "description": "Базовый URL провайдера LLM",
      "required": true,
      "validation": {
        "format": "uri"
      },
      "example": "https://api.openai.com/v1"
    },
    {
      "name": "DB_MCP_URL",
      "type": "string",
      "default": "",
      "description": "HTTP URL MCP-сервера для работы с профилями компаний",
      "required": true,
      "validation": {
        "format": "uri"
      },
      "example": "http://localhost:28001/mcp"
    },
    {
      "name": "GOSPLAN_MCP_URL",
      "type": "string",
      "default": "",
      "description": "HTTP URL MCP-сервера для поиска закупок ЕИС",
      "required": true,
      "validation": {
        "format": "uri"
      },
      "example": "http://localhost:28003/mcp"
    },
    {
      "name": "AGENT_NAME",
      "type": "string",
      "default": "PurchaseMatcher",
      "description": "Отображаемое имя A2A-агента",
      "required": false,
      "example": "PurchaseMatcher"
    },
    {
      "name": "AGENT_DESCRIPTION",
      "type": "string",
      "default": "Агент подбирает закупки ЕИС для заданной компании по профилю и запросу",
      "description": "Описание агента в A2A-карточке",
      "required": false,
      "example": "Агент подбирает закупки ЕИС для заданной компании по профилю и запросу"
    },
    {
      "name": "AGENT_HOST",
      "type": "string",
      "default": "0.0.0.0",
      "description": "Хост для запуска A2A HTTP-сервера",
      "required": false,
      "validation": {
        "format": "hostname"
      },
      "example": "0.0.0.0"
    },
    {
      "name": "AGENT_PORT",
      "type": "integer",
      "default": 8002,
      "description": "Порт A2A HTTP-сервера",
      "required": false,
      "validation": {
        "min": 1,
        "max": 65535
      },
      "example": 8002
    },
    {
      "name": "AGENT_URL",
      "type": "string",
      "default": "",
      "description": "Полный URL агента (если не указан, собирается из AGENT_HOST и AGENT_PORT)",
      "required": false,
      "validation": {
        "format": "uri"
      },
      "example": "http://0.0.0.0:8002"
    },
    {
      "name": "AGENT_VERSION",
      "type": "string",
      "default": "v1.0.0",
      "description": "Версия агента, отображаемая в A2A-карточке",
      "required": false,
      "example": "v1.0.0"
    }
  ],
  "metadata": {
    "version": "1.0.0",
    "description": "Переменные окружения для запуска агента PurchaseMatcher через A2A",
    "usage": {
      "docker": "Передавайте переменные через --env-file или -e",
      "local": "Создайте .env в корне agent-purchase-matcher или экспортируйте переменные в окружение"
    },
    "notes": [
      "LLM_* обязательны для работы агента через LangChain",
      "DB_MCP_URL и GOSPLAN_MCP_URL должны указывать на доступные MCP-сервера",
      "AGENT_URL вычисляется автоматически, если не задан"
    ]
  }
}
