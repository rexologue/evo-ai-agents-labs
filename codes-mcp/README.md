# codes-mcp

MCP сервер, который отдает коды и наименования по справочникам ОКПД2 и регионов.

## 🚀 Возможности
- `get_okpd2_codes` — таблица кодов и наименований ОКПД2.
- `get_regions_codes` — таблица кодов и наименований регионов.

## 🔧 Переменные окружения

Все переменные задаются через `.env` (см. `.env.example`) или флаги контейнера.

- `MCP_HOST` — хост HTTP транспорта MCP (обязательно)
- `MCP_PORT` — порт HTTP транспорта MCP (обязательно)
- `LOG_LEVEL` — уровень логирования (`DEBUG`/`INFO`/`WARNING`/`ERROR`/`CRITICAL`)

## 🚀 Запуск
```bash
docker buildx build --platform linux/amd64 -t codes-mcp -f Dockerfile ..
docker run --rm --network host --env-file .env codes-mcp
```

После старта сервер доступен по адресу `http://<MCP_HOST>:<MCP_PORT>/mcp` с транспортом `streamable-http`.

## 📖 Использование инструментов
- `get_okpd2_codes()` — возвращает таблицу ОКПД2 и структурированные данные.
- `get_regions_codes()` — возвращает таблицу регионов и структурированные данные.

Полное описание аргументов доступно в `mcp_tools.json`.
